apiVersion: v1
kind: Service
metadata:
  name: helm-wrapper-svc
  labels:
    app: helm-wrapper
spec:
  type: LoadBalancer
  ports:
    - port: 80
      protocol: TCP
  selector:
    app: helm-wrapper